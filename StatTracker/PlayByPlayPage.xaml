<Page
    x:Class="McKnight.StatTracker.PlayByPlayPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:McKnight.StatTracker"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="200" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="5*" />
        </Grid.RowDefinitions>
        <StackPanel Grid.ColumnSpan="2" Orientation="Horizontal" Grid.Row="0">
            <TextBlock Text="{x:Bind Game.HomeTeamId}" />
            <TextBlock Text="{x:Bind Game.VisitingTeamId}" />
        </StackPanel>
        <StackPanel Orientation="Vertical" Grid.Column="1" Grid.Row="1">
            <TextBlock Text="Umpires:" />
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Home:" />
                <TextBlock Text="{x:Bind Game.HomePlateUmpire}" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
